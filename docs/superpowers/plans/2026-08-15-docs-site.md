# Docs Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Material-for-MkDocs site (mocapi's stack) publishing nessy's concepts/guides/examples/reference to GitHub Pages.

**Architecture:** `mkdocs.yml` at root, content under `docs/` (with `docs/superpowers/` excluded from the site), `docs.yml` Pages workflow. Content is curation: rewrite existing READMEs/specs prose into guide voice, checked against current main for truth.

**Tech Stack:** mkdocs-material==9.7.7 (pip), GitHub Pages via actions.

**Spec:** `docs/superpowers/specs/2026-08-15-docs-site-design.md` — §2 is the complete page inventory and nav; §3 the binding page rules. Every task reads the spec first.

## Global Constraints

- Every page obeys spec §3: snippets verified against current source (exact package/type/method names — grep before you write), repo voice, admonitions for sharp edges, "Where next" links, never link into docs/superpowers.
- `mkdocs build --strict` green after every task (install once: `pip3 install --user 'mkdocs-material==9.7.7'`; run `mkdocs build --strict` from the worktree root; if pip/network is unavailable, note it in the report and rely on the reviewer — do NOT skip writing).
- Java snippets in docs are illustrative but must compile in spirit: builder chains and signatures match main. When lifting from a README, re-verify — READMEs were truth-audited but APIs moved since (PipelineMemory is the ONLY shipped Memory; Memory.windowed/TranscriptMemory/SummarizingMemory no longer exist).
- No edits outside: mkdocs.yml, .github/workflows/docs.yml, docs/** (excluding docs/superpowers/**), root README.md (Task 5 only).
- Before commit: `./mvnw -q license:format -Plicense && ./mvnw -q spotless:apply` (touches nothing here but stays the ritual); trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Never push.

---

### Task 1: Scaffolding — mkdocs.yml, workflow, home

**Files:** Create `mkdocs.yml` (root), `.github/workflows/docs.yml`, `docs/index.md`.

- [ ] **Step 1:** Read mocapi's `~/IdeaProjects/mocapi/mkdocs.yml` and `.github/workflows/docs.yml` as the templates. Write nessy's `mkdocs.yml` per spec §1 (site coordinates, stock Material theme with light/dark toggle and mocapi's feature list, mocapi's markdown extensions, `exclude_docs: /superpowers/`) with the FULL nav from spec §2 — nav entries for every page all tasks will create (strict build will fail until they exist, so create every page as a stub file with its H1 + one-line summary in THIS task; later tasks replace stubs with content).
- [ ] **Step 2:** Write `docs/index.md` — the landing: what nessy is (durable agent framework: crash-safe conversations, parked waits, replayable turns), the twenty-line getting-started teaser, cards/links to Concepts/Guides/Examples/Reference.
- [ ] **Step 3:** `.github/workflows/docs.yml` — mocapi's shape verbatim (paths filter, python 3.13, pinned pip install, `mkdocs build`, upload-pages-artifact, deploy-pages with the pages environment); license header comment style matching nessy's other workflows.
- [ ] **Step 4:** `mkdocs build --strict` green (all stubs present). Commit `docs: the site scaffold — nav, theme, workflow, and a home page`.

### Task 2: Concepts (six pages)

**Files:** Replace stubs `docs/concepts/{durable-loop,tools-and-grants,parks-and-callbacks,memory-and-the-pipeline,planning,storage}.md`.

- [ ] **Step 1:** Sources per page — read before writing: root README + core javadoc (durable-loop); Tool/ToolGrant/UsagePolicy javadoc + nessy-tool-mcp README (tools-and-grants); Agent door javadoc + WrongAgentException + callback-doors spec prose (parks-and-callbacks); Memory/PipelineMemory/ContextHydrator/ContextTransformer javadoc + context-pipeline spec §2 (memory-and-the-pipeline); PlanTools/PlanStore javadoc + spec §3 + chat-cli/scout READMEs (planning); the five door SPIs + nessy-jdbc/nessy-tck READMEs (storage).
- [ ] **Step 2:** Write the six pages per spec §3 rules. planning.md covers: the tool contract (full list every time, one IN_PROGRESS, empty clears — and that cleared = absent), the injected `<current-plan>` block verbatim, replay idempotency, the console checklist, when NOT to grant it.
- [ ] **Step 3:** `mkdocs build --strict`; commit `docs: the concepts — six pages of vocabulary`.

### Task 3: Guides part 1 (getting-started, providers, durable-persistence, console-apps, spring-boot)

**Files:** Replace those five stubs under `docs/guides/`.

- [ ] **Step 1:** Sources: hello + chat-cli mains (getting-started); nessy-model-env README + EnvModelProviders (providers); nessy-jdbc README + JdbcPersistence/JdbcDialect javadoc (durable-persistence); nessy-console README + ConsoleRepl/Renderer/Approver + the .plan() §9 amendment (console-apps); starter/autoconfigure READMEs + NessyProperties (spring-boot).
- [ ] **Step 2:** Write; every wiring snippet copied from a real main or reconstructed and name-checked against source.
- [ ] **Step 3:** `mkdocs build --strict`; commit `docs: guides, first five — from twenty lines to durable`.

### Task 4: Guides part 2 (mcp-clients, planning-your-agents-work, summarizing-memory, triggers, observability) + Examples tour

**Files:** Replace those five stubs under `docs/guides/` + `docs/examples/index.md`.

- [ ] **Step 1:** Sources: nessy-tool-mcp README + scout (mcp-clients); chat-cli/scout wiring + the live-transcript lessons in scout README (planning-your-agents-work); SummarizingHydrator javadoc (summarizing-memory — note the class carries the watermark story now); dispatcher/order-desk/night-watchman/chat-web READMEs (triggers); HarnessBuilder observations + TurnObserver/listener javadoc (observability — honest about no per-stage spans yet, per spec).
- [ ] **Step 2:** Write guides + the examples tour table (seven examples, one paragraph each, repo links).
- [ ] **Step 3:** `mkdocs build --strict`; commit `docs: guides, second five, and the examples tour`.

### Task 5: Reference + README link + final strict pass

**Files:** Replace stubs `docs/reference/{configuration,tck}.md`; modify root `README.md` (docs-site link near the top); verify nav's external links (CHANGELOG, source).

- [ ] **Step 1:** configuration.md: gather every `nessy.*` property from NessyProperties + autoconfigure javadoc into one table (name, default, effect). tck.md: certifying a backend — depend on nessy-tck, extend the six contracts, public-@Test rule called out in an admonition, vendor-matrix pattern.
- [ ] **Step 2:** Root README: one line + badge-style link to https://jwcarman.github.io/nessy/ (site goes live after the owner flips Pages to "GitHub Actions" — README wording must not break while the toggle is pending; plain link is fine).
- [ ] **Step 3:** Full `mkdocs build --strict`; skim `site/` output for nav completeness (every spec-§2 page present, no stub text remaining — grep the built site for the stub marker phrase used in Task 1).
- [ ] **Step 4:** Commit `docs: reference pages and the front-door link — the site is whole`.

## Self-review notes
- Stub-first strategy makes strict builds green from Task 1 onward; Task 5's grep-for-stub-marker guarantees no stub ships.
- Page inventory matches spec §2 exactly (16 content pages + index + examples tour + 2 reference).
- API-truth risk concentrated in Tasks 2-4; the deleted-Memory-facades warning is in Global Constraints because READMEs may predate the consolidation.
