# Scout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `nessy-examples/scout` — the tool-import showcase: a chat-cli-style terminal agent researching GitHub repos via DeepWiki's public MCP server, one imported tool behind the console approval gate.

**Architecture:** Two tasks — the module with its offline scripted test, then paperwork. Sequential.

**Spec:** `docs/superpowers/specs/2026-08-15-scout-design.md` — binding.

## Global Constraints

- Offline `./mvnw -q clean verify` green after every task (scout's test is default-build: in-process MCP server + ScriptedModelProvider, no key/network/Docker).
- Before every commit: `./mvnw license:format -Plicense && ./mvnw spotless:apply`, re-stage. No IDE metadata, no suppressions, no star imports, no mocking libraries, prose snake_case test names, S5778/S5841, example switches keep tolerant defaults.
- Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: The module

**Files:** `nessy-examples/scout/` (aggregator line in `nessy-examples/pom.xml`; pom mirroring chat-cli's shape + `nessy-tool-mcp` dependency; `Scout.java` main per spec §2 — read chat-cli's `AnthropicChat` for the REPL idiom, delta rendering, `ConsoleApprover` handling and copy-vs-reuse note; the DeepWiki wiring verbatim from the spec with the SDK builder idiom verified against the jar; a package-private construction seam the test shares); offline `ScoutTest` per spec §3 (in-process real SDK server from `nessy-tool-mcp`'s test pattern — that pattern lives in another module's src/test, so REPRODUCE the minimal server+transport pairing locally in scout's test tree, noting the provenance); logback in family convention.

- [ ] RED: the scripted test against a stub main-seam; GREEN: the module. Offline reactor green. Manual smoke optional (needs key+network) — if run, transcript in the report; if not, say so.
- [ ] Commit: `feat: scout — the agent that reads other people's code`

### Task 2: Paperwork

Module README per spec §4 (lesson, grants block, run command, key+network, DeepWiki covenant, approval transcript); root README examples row + count arithmetic; CHANGELOG `### Added`. Full offline sweep + container sweep (`-Dnessy.excludedGroups=live`) end to end.

- [ ] Commit: `docs: scout reports in — the import papers get a face`

---

## Self-Review Notes (already applied)

- Task 1 verifies DeepWiki's real tool names once during development and records them (spec §2) — the README documents them; drift fails loud at connect by design.
- The test shares the demo's actual grant table via the construction seam — a parallel wiring copy in the test would prove nothing (spec §3).
- The in-process server pairing is reproduced in scout's own test tree, not imported from nessy-tool-mcp's src/test (no test-jar publishing games for an example).
