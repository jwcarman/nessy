<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2026 James Carman

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jwcarman.nessy</groupId>
    <artifactId>nessy-examples</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>nessy-example-dispatcher</artifactId>
  <name>Nessy Example: Dispatcher</name>
  <description>A Spring Boot HTTP dispatcher: fire-and-forget signals and token callbacks over durable parks</description>

  <properties>
    <!-- Examples are never published: no jar, sources, or javadoc leaves this module. -->
    <maven.deploy.skip>true</maven.deploy.skip>

    <spring-boot.version>4.1.0</spring-boot.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Spring enters the reactor only here: this import, and nowhere else in the build,
           exactly the chat-web discipline. -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <!-- nessy-store-jdbc brings no Spring JDBC autoconfiguration of its own (only the postgresql
         driver, see its own pom); DataSourceAutoConfiguration (and therefore
         JdbcPersistenceAutoConfiguration, which is @ConditionalOnBean(DataSource.class)) needs
         this starter on the classpath to ever build a DataSource bean at all — chat-web's same
         dependency, for the same reason. Without it there is no ConversationStore, no Parks bean,
         and no Memory bean either: DispatcherConfig#agent's Memory parameter would then fail to
         resolve at all, so startup fails loudly, not silently. The Parks half is the one silent
         risk: HarnessBuilder defaults an unset Parks to Parks.inMemory() rather than refusing to
         build, so losing this dependency would quietly trade this module's whole "JdbcParks is
         load-bearing" story (spec §3) for parks that vanish on process exit — the harness would
         still build and run, just not durably. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <!-- Boot 4's default web stack auto-configures a Jackson 3 (tools.jackson) ObjectMapper;
         nessy-core and every bean in this module are written against classic Jackson 2
         (com.fasterxml.jackson.databind). This module pulls Boot's own backward-compat
         auto-configuration for that generation back in (chat-web's precedent). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-jackson2</artifactId>
    </dependency>
    <!-- Boot 4's default logging starter only bridges Log4j2/JUL onto SLF4J; it ships no SLF4J
         binding of its own. Compile-scope, as in chat-web: examples are runnable apps, and an
         app picks its own SLF4J provider. -->
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- Makes `mvn spring-boot:run` start (and stop) docker-compose.yml automatically: postgres
         only (start-only lifecycle, see application.yaml). Optional: an application jar built
         from this module never requires it on its own runtime classpath. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-docker-compose</artifactId>
      <scope>runtime</scope>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.jwcarman.nessy</groupId>
      <artifactId>nessy-spring-boot-starter</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jwcarman.nessy</groupId>
      <artifactId>nessy-model-anthropic</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jwcarman.nessy</groupId>
      <artifactId>nessy-store-jdbc</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
      <exclusions>
        <!-- The house bans mocking libraries (no test doubles from a framework); Boot's test
             starter pulls Mockito in transitively, so it is excluded explicitly. -->
        <exclusion>
          <groupId>org.mockito</groupId>
          <artifactId>mockito-core</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- Boot 4 split TestRestTemplate out of spring-boot-test into its own module; the starter
         above no longer pulls it in transitively. TestRestTemplateTestAutoConfiguration itself
         needs RestTemplateBuilder (spring-boot-restclient), which the RestClient-first web
         starter no longer brings in either. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-resttestclient</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-restclient</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- SignalController, CallbackController, and IncidentController resolve
           @PathVariable-annotated parameters by matching the compiled parameter name itself;
           nessy-parent's own compiler config never sets -parameters (only Boot's starter-parent
           does, which this reactor doesn't inherit from), so without this override every such
           endpoint 500s at request time — javac strips parameter names by default (chat-web's
           pom comment, same fix). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <parameters>true</parameters>
        </configuration>
      </plugin>
      <!-- Version and repackage-goal wiring come from the BOM's pluginManagement (imported
           above); no repackage execution is declared here — `spring-boot:run` needs none. -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
