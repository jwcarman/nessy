target/
*.iml
site/
.idea/
.DS_Store
.classpath
.project
.settings/
.factorypath

# SDD/agent workspace scratch — never committed
.superpowers/

# Agent worktrees — never committed
.claude/worktrees/
.worktrees/

# Browser debris from Playwright MCP runs: console logs and page snapshots
# written into the working tree while driving a real browser. Never source.
.playwright-mcp/
